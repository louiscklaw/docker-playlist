{
  "name": "docker-healthcheck-tryout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gitSync": "git fetch --all && git pull",
    "gitUpdate": "git add . && git commit -m\"update docker-healthcheck-tryout\" && yarn gitSync && git push",
    "docker_rebuild": "docker compose rm -s -f -v && docker compose up --build -d "
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
