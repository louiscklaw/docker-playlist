{
  "name": "docker-matomo-tryout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gitSync": "git fetch --all && git pull",
    "gitUpdate": "yarn gitSync && git add . && git commit -m\"update docker-matomo-tryout\" && git push &",
    "docker_rebuild": "docker-compose build && docker-compose up -d && docker-compose logs -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^13.0.2"
  }
}
